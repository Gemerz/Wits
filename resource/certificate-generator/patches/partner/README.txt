#How to patch?
 if Tizen SDK is installed in default path (~/tizen-sdk or c:\tizen-sdk).
  - Run patch.sh (Linux, MAC) or patch.bat (Windows) 
 If Tizen SDK is installed in different path,
  - Run patch.sh(or patch.bat) $SDK_PATH
    Ex) ./patch.sh ~/project/tizen-sdk
